<%= form_with(model: video) do |form| %>
  <% if video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
        <% video.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="input-field">
    <%= form.label :wistia %>
    <%= form.text_field :wistia, :placeholder => 'Link', :length => '10' %>
  </div>

  <div class="input-field">
    <%= form.label :title %>
    <%= form.text_field :title, :placeholder => 'Title', :length => '10'%>
  </div>

  <div class="from-floating">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>-->

  <div class="container">
  <div class="col">
  <div class="card">
  <div class="card-body">
  <div class="form-floating">
    <%= form.text_field :wistia, :class => 'form-control', :placeholder => 'Link', :id => 'floatingInput', :length => '10' %>
    <label for="floatingInput"><%= form.label :wistia %></label>
  </div>
  <br>
  <div class="form-floating">
    <%= form.text_field :title, :class => 'form-control', :placeholder => 'Title', :id => 'floatingInput', :length => '20' %>
    <label for="floatingInput"><%= form.label :title %></label>
  </div>
  <br>
  <div class="form-floating">
    <%= form.text_area :description, :class => 'form-control', :placeholder => 'Sum up the video', :id => 'floatingTextarea2', :style => 'height: 100px' %>
    <label for="floatingTextarea2"><%= form.label :description %></label>
  </div>
  <br>
  <div class="d-grid gap-2">
    <button class="btn btn-primary" type="submit">SUBMIT</button>
    <a href="<%= videos_path %>" class="btn btn-outline-primary" name="action">BACK</a>
  </div>
  </div>
  </div>
  </div>
  </div>
<% end %>
